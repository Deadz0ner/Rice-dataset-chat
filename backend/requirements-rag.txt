# Optional packages for the future RAG implementation.
# These are not required for the current scaffold because
# chunking / embeddings / vector indexing are still TODO placeholders.

faiss-cpu==1.11.0.post1
chromadb==1.0.7
